# Copy to frontend/.env
# Where the Kairos API runs (production: your Railway/Render URL)
VITE_API_URL=http://localhost:8000

# Only needed when the backend has a real STRIPE_SECRET_KEY (pk_test_...)
# Leave empty to use mock payments
VITE_STRIPE_PUBLISHABLE_KEY=
