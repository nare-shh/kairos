# Pinned to versions that ship wheels for Python 3.12 (Docker image) AND 3.13 (local dev)

# Web framework — FastAPI is the fastest Python API framework
fastapi==0.115.6

# ASGI server — runs FastAPI in production (like a waiter serving requests)
uvicorn[standard]==0.32.1

# ORM — lets us write Python instead of raw SQL
# [asyncio] pulls in greenlet, which SQLAlchemy does NOT install on Python 3.13 by default
sqlalchemy[asyncio]==2.0.36

# Async PostgreSQL driver — connects SQLAlchemy to Postgres asynchronously
asyncpg==0.30.0

# Database migrations — tracks and applies schema changes
alembic==1.14.0

# Sync PostgreSQL driver — required by Alembic (async driver can't run migrations)
# asyncpg is used by the running app; psycopg2 is used only by alembic
psycopg2-binary==2.9.10

# Redis client — for caching and pub/sub
redis==5.2.1

# Kafka client — for event streaming
aiokafka==0.12.0

# Password hashing — use bcrypt directly (passlib 1.7.4 is incompatible with bcrypt 5.x)
bcrypt==4.2.1

# JWT tokens — for stateless authentication
python-jose[cryptography]==3.3.0

# Environment variable management — reads from .env file
python-dotenv==1.0.1

# Data validation — FastAPI uses this under the hood
pydantic==2.10.4
pydantic-settings==2.7.0

# EmailStr validation (FastAPI >= 0.112 no longer bundles it)
email-validator==2.2.0

# Stripe payments
stripe==9.9.0

# HTTP client — for internal service calls
httpx==0.28.1

# Rate limiting — prevent API abuse
slowapi==0.1.9

# Structured JSON logging — parseable by tools like Datadog, CloudWatch, Railway logs
python-json-logger==2.0.7

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
# In-memory Redis for tests — no Redis server needed to run the test suite
fakeredis==2.26.2
